<h2>Blog Posts</h2>
<% @posts.each do |p| %>
  <div>
    <%= link_to p.title, post_path(p) %>
  </div>
  <div>
    Category: <%= p.category_or_unknown %>
  </div>
  <div>
    <%= p.body %>
  </div>
<% end %>
