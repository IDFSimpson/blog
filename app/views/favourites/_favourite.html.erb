<% if user_signed_in? && @post.favourited_by?(current_user)%>
  <%= link_to "Remove Favourite", post_favourite_path(@post, @post.favourite_for(current_user)), method: :delete %>
<% else %>
  <%= link_to "Favourite Post", post_favourites_path(@post), method: :post %> 
<% end %>
